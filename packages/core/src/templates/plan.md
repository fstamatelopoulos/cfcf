# Implementation Plan

<!--
The dev agent creates and evolves this plan across iterations. The plan is
the shared source of truth between iterations: cfcf runs one iteration per
agent process, so the plan is also the iteration schedule.

Each iteration executes ONE pending chunk. Before exiting, the dev agent
marks completed items `[x]` with a brief note (files touched, tests added,
deviations). The next iteration picks up from the next pending item.

Map phases to concrete iterations, e.g.:

## Iteration 1 -- Foundation
- [ ] Task A
- [ ] Task B

## Iteration 2 -- Core features
- [ ] Task C
- [ ] Task D
-->

