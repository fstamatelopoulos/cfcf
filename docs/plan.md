# cf² Development Plan

*cfcf and cf² are used interchangeably. Both are pronounced "cf square." `cfcf` in code; cf² in docs.*

**Status:** Living Document
**Date:** April 2026 — May 2026
**Authors:** Fotis Stamatelopoulos, Claude

---

## Current Status

**Iteration 5 complete** (2026-04-12 → 2026-05-02). All 16 items shipped:
- **Clio cross-workspace memory** (5.7, 5.10–5.13) — local SQLite-backed agent memory layer with FTS5 + hybrid search, soft-delete + versioning, audit log, agent-parity API surface
- **Solution Architect, Product Architect, Help Assistant, Reflection roles** (5.6, 5.8, 5.14) — five non-interactive + two interactive agent roles
- **npm-format distribution + first public release on npmjs.com** (5.5, 5.5b) — `@cerefox/codefactory` published with OIDC trusted publishing + sigstore provenance
- **Structured pause actions** (item 6.25 promoted from iter-6, shipped 2026-05-02) — five-action `ResumeAction` enum with contextual UI button matrix
- **Supporting UX bundle** (5.1–5.4, 5.9, 5.15) — autoReviewSpecs/autoDocumenter config flags, branch auto-cleanup, CLI self-hosting, embedded runtime assets, editable global settings, PA+HA web-UI investigation

**Iteration 6 in progress.** Active scope locked 2026-05-02 after a per-item cleanup pass:
- **ADLC research** (6.11, headline) — extend cfcf to non-coding iterative work. Research-only, output `docs/research/adlc-extension-design.md`. Implementation deferred to iter-7+.
- **Scheduled execution research** (6.13) — pairs with 6.11. Research-only, output `docs/research/scheduled-execution-design.md`.
- **Clio rationalisation across agent roles** (6.9 reframed) — standardise how agents query/write Clio.
- **CLI ↔ web parity audit** (6.12) — absorbs the originally-separate `cfcf log` question (6.2).
- **Web UI polish**: Clio browser tab (6.18), new-version notification (6.20), per-role model dropdowns (6.26).
- **Cerefox-parity Clio improvement**: FTS title boosting (6.24).
- **Plus**: 6.19 partial (pre-warm-during-installer remaining); 6.8 (custom directions per role) blocked on 6.11 and picked up after.

**Dropped from iter-6** (status ⏸ in their rows): 6.1 diff viewer (superseded by F.13), 6.2 `cfcf log` CLI (subsumed by 6.12), 6.10 sandbox research (moved to F.5 pending multi-tenant trigger), 6.15 sqlite-vec HNSW (moved to F.18 pending corpus-growth trigger).

The **Backlog** section below carries durable records for everything not on the active plan, with `F.x` numbering and original 6.x numbers preserved for traceability.

**Latest release:** v0.17.0 on npmjs.com (2026-05-02) — first npm-published release after the v0.16.x dogfood-only series.

---

## Guiding Principles for Iteration Planning

- Each iteration should produce a working, testable artifact.
- We build the simplest version first, then layer complexity.
- Agents run as local processes in the user's dev environment -- no containers.
- Two initial agents: Claude Code (dev) + Codex (judge), both configurable.
- The cfcf server is the backbone from the start -- it manages execution and exposes a CLI API.
- Each agent iteration is a single non-interactive execution: the agent reads context, works, produces a handoff document + signal file, and exits.
- Even failed iterations are committed. The next agent decides whether to backtrack.
- All cfcf-generated files live in the repo (tracked in git). Agent logs backed up to `~/.cfcf/`.
- Feature branch per iteration, merged to main after iteration completes.

---

## Resolved Design Decisions

These were open questions that have been resolved through brainstorming.

### Execution model: Local processes, not containers

Agents run as local processes in the user's normal dev environment. No Docker, no containers. This reuses existing agent authentication, git credentials, and tooling. Isolation is achieved via git branches and agent instructions (guardrails). Containers are a future option.

### Agent interaction: Fire-and-forget with full log capture

cfcf spawns the agent as a single non-interactive process. The agent reads context from files, executes, and exits. cfcf captures all stdout/stderr via streaming. No mid-run interaction for v0.1. Full logs are stored in cfcf's external memory for judge evaluation and user review.

### Context delivery: File-based

All context is delivered via files in the repo: CLAUDE.md (or agent equivalent), a `cfcf-docs/` folder with problem definition, iteration history, process instructions, etc. The agent is instructed which files to read and in what order. No stdin/prompt component for now.

### Iteration state on failure: Commit everything

Failed iterations are committed to the repo. The next iteration's agent analyzes commits and docs and may backtrack using git. cfcf also maintains copies of key files and logs in its external memory (~/.cfcf/).

### Server from the start

cfcf is a server that manages workspaces, iterations, and agent processes. The CLI (and later web GUI) are clients. The server is always-on, serving a REST/SSE API. Initially supports a single workspace execution; designed to support multiple workspaces in parallel later.

### Fresh agent session per iteration

No session continuity between iterations. Each iteration spawns a fresh agent process. Context comes from files, not from agent memory. This avoids carrying forward bad assumptions.

### Git strategy: Feature branch per iteration

Each iteration gets its own feature branch off main. On normal completion, the branch is merged to main (via PR or direct merge). GitHub is the only supported remote for v0.1. This is a "dark factory" model -- cfcf-controlled agents are the primary committers, though human developers can coexist on the same repo using different branches.

### All files in the repo, no external persistent memory (for now)

All cfcf-generated files (assessments, signals, plans, logs summaries) are tracked in git under `cfcf-docs/`. Agent stdout/stderr logs are backed up to `~/.cfcf/` (too large for repo). An external persistent memory layer is deferred -- it will appear organically when needed.

### First-run interactive configuration

On first execution (no config file found), cfcf runs agent detection (Claude Code, Codex), asks user for defaults (dev agent, judge agent, max iterations, etc.), explains `--dangerously-skip-permissions`, and stores config in the platform's standard config directory (e.g., `~/.config/cfcf/` on Linux). See `design/technical-design.md` section 4.11.

### Process template: copied into repo, versioned in git

cfcf ships default process templates. On `cfcf init`, the selected template is copied into the repo under `cfcf-docs/`. The user or agents can modify it -- changes are tracked in git. Which template was used is recorded in workspace config.

---

## Open Design Questions

### Q1: Signal file reliability

Malformed or missing signal files are treated as anomalies -- cfcf alerts the user and does not try to infer from Markdown. Resolved, but worth monitoring in practice.

### Q2: Token bootstrapping cost mitigation

Each iteration re-reads all context. Strategies to manage this:
- Tightly defined process docs with explicit read order
- SLM-compressed summaries of previous iterations
- Clear "must read" vs "reference only" context tiers
- Keeping handoff documents concise

**Decision:** Ongoing -- will validate with real agent iterations in Iteration 2.

---

## Iteration Plan

Status legend: ✅ Done · 🟡 In progress · ❌ Not started · ⏸ Deferred · ⚠️ Blocked

The tables below are the authoritative view of iteration progress. The **Notes** column records the outcome, commit reference, or reason for deferral — read these first when reorienting between sessions.

---

### Iteration 0: Project Scaffolding + Server Skeleton

**Goal:** Monorepo structure, build pipeline, basic server, basic CLI that talks to the server. No agents, no orchestration.

| # | Status | Title | Notes |
|---|--------|-------|-------|
| 0.1 | ✅ | Bun monorepo with `packages/core`, `packages/server`, `packages/cli` | Workspace aliases set up: `@cfcf/core`, `@cfcf/server` |
| 0.2 | ✅ | Hono server with `/api/health`, `/api/status`, `/api/config` | Bun.serve, single process |
| 0.3 | ✅ | Commander.js CLI: `cfcf`, `cfcf init`, `cfcf server start/stop/status` | |
| 0.4 | ✅ | First-run interactive configuration | Agent detection (Claude Code, Codex), dependency check, permission acknowledgment, XDG-compliant config storage |
| 0.5 | ✅ | Agent adapter interface + Claude Code + Codex adapters | Plugin contract defined in `types.ts` |
| 0.6 | ✅ | Test suite structure (Bun test) | Colocated test files |
| 0.7 | ✅ | `decisions-log.md` | Captures non-obvious decisions and failed experiments |

**Tag:** v0.0.0

---

### Iteration 1: Project Management + Process Manager + Git + CI

**Goal:** cfcf can manage projects, spawn local processes, capture logs, manage git branches. No AI agent yet — prove the plumbing.

| # | Status | Title | Notes |
|---|--------|-------|-------|
| 1.1 | ✅ | Project CRUD + storage | `~/.cfcf/projects/<id>/config.json`; `currentIteration` counter |
| 1.2 | ✅ | CLI: `cfcf project init/list/show/delete` | |
| 1.3 | ✅ | CLI: `cfcf config show/edit` | |
| 1.4 | ✅ | Process manager | `Bun.spawn()`, streaming stdout/stderr, kill, timeout |
| 1.5 | ✅ | Log storage | `~/.cfcf/logs/<project-id>/iteration-NNN-<role>.log` |
| 1.6 | ✅ | Git manager | Branch (`cfcf/iteration-N`), commit, diff, reset, push, merge |
| 1.7 | ✅ | Server: project CRUD, `POST /api/projects/:id/iterate`, SSE logs | |
| 1.8 | ✅ | `cfcf run --project <name> -- <command>` (manual mode) | Branch → spawn → capture → commit |
| 1.9 | ✅ | Build script + GitHub Actions CI | Originally `bun build --compile` self-contained binary across darwin-arm64 / darwin-x64 / linux-x64 (shipped v0.5.0–v0.9.0). Superseded in v0.10.0 by item 5.5's npm-format CLI tarball — the `--compile` path hit unworkable limits with the heavy native deps onnxruntime-node + sharp. See `docs/decisions-log.md` 2026-04-26. |
| 1.10 | ✅ | Reliable `cfcf server stop` via PID file | |

**Tag:** v0.1.0

---

### Iteration 2: Problem Pack + Context Assembly + One-Shot Agent Run

**Goal:** cfcf reads a Problem Pack, assembles context (CLAUDE.md + `cfcf-docs/`), spawns a real AI coding agent, runs one iteration, captures results. First end-to-end useful flow.

| # | Status | Title | Notes |
|---|--------|-------|-------|
| 2.1 | ✅ | Problem Pack parser | Reads problem.md, success.md, constraints.md, hints.md, context/ |
| 2.2 | ✅ | Context assembler | Tiered context; writes CLAUDE.md + cfcf-docs/ templates into repo |
| 2.3 | ✅ | Claude Code adapter: `checkAvailability`, `buildCommand`, `instructionFilename` | `claude --dangerously-skip-permissions -p "..."` (later added `--verbose` in iteration 4) |
| 2.4 | ✅ | Codex adapter | `codex exec -a never -s danger-full-access "..."` (updated in iteration 3 from deprecated `--approval-mode`) |
| 2.5 | ✅ | `cfcf-docs/` file templates | process.md, handoff, signals JSON, plan, decision log |
| 2.6 | ✅ | `cfcf project init` scaffolds `problem-pack/` | |
| 2.7 | ✅ | `cfcf run` agent mode — full end-to-end flow | Context assembly → agent spawn → handoff parse → commit |
| 2.8 | ✅ | Example Problem Pack | `problem-packs/calculator/` (single iteration) |
| 2.9 | ✅ | Async iteration runner | Server returns 202, CLI polls for status. Fixes HTTP timeout on long runs |
| 2.10 | ⏸ | Token measurement | Deferred — agents' token reporting is not standardized |

**Tag:** v0.2.0

---

### Iteration 3: Iteration Loop + Judge + Architect + Documenter + Human-on-the-Loop (MVP)

**Goal:** Multiple iterations in a loop, with a judge evaluating after each, architect reviewing Problem Pack, documenter producing final docs. Pause/resume/stop. This is the MVP.

| # | Status | Title | Notes |
|---|--------|-------|-------|
| 3.1 | ✅ | Iteration loop controller + state machine | prepare → dev → commit → judge → commit → decide cycle; loop state machine |
| 3.2 | ✅ | Judge runner | Spawn, parse signals + assessment, archive to `iteration-reviews/` |
| 3.3 | ✅ | Signal file schemas | `cfcf-iteration-signals.json` (dev), `cfcf-judge-signals.json` (judge); validated by cfcf |
| 3.4 | ✅ | Human-on-the-loop: `pauseEvery`, `cfcf resume`, `cfcf stop` | Pause cadence + signal-driven pause (user_input_needed). Resume with `--feedback` |
| 3.5 | ✅ | Solution Architect + `cfcf review` | Generates architect-review.md, plan.md outline, docs/ stubs |
| 3.6 | ✅ | Documenter role (`cfcf document`, auto post-SUCCESS) | Produces docs/architecture.md, api-reference.md, setup-guide.md, README.md |
| 3.7 | ✅ | Model selection per role | `cfcf init` asks for dev/judge/architect/documenter agent + model |
| 3.8 | ✅ | Loop state persistence to disk | `~/.cfcf/projects/<id>/loop-state.json`; survives server restarts |
| 3.9 | ✅ | `cfcf status --project <name>` | Current loop state + iteration progress |
| 3.10 | ✅ | Three-layer docs strategy (architect stubs → dev maintains → documenter polishes) | |
| 3.11 | ✅ | Elapsed time counter in CLI polling | Replaces dots |
| 3.12 | ✅ | Judge retry on same branch after failure | Fixes "server restart killed judge" scenario |

**Tag:** v0.3.0

---

### Iteration 4: Web GUI + Unified Agent-Run Model

**Goal:** React web GUI for monitoring and control. Unified state machine across review/loop/document. Start preparing for iteration 5 infrastructure.

| # | Status | Title | Notes |
|---|--------|-------|-------|
| 4.1 | ✅ | `packages/web`: React + Vite web GUI | Dashboard, project detail, 4 tabs (Status/History/Logs/Config), dark theme. Served by Hono via serveStatic |
| 4.2 | ✅ | Log viewer: real-time SSE streaming + historical | Handles 50K+ line logs via single `<pre>`. Auto-scroll with top/bottom buttons |
| 4.3 | ✅ | LoopControls + FeedbackForm + PhaseIndicator | Visual state machine, phase transitions, feedback injection on pause |
| 4.4 | ✅ | Loop events SSE endpoint | `GET /api/projects/:id/loop/events` |
| 4.5 | ✅ | Git merge strategy: `--no-ff` | Preserves iteration boundaries in git history. One-line change in `git-manager.ts`. |
| 4.6 | ✅ | Project history + unified log streaming | Persistent `history.json`; sequence-numbered logs (architect-001.log, etc.); generic `/logs/:filename` endpoint; ProjectHistory unified timeline UI |
| 4.7 | ✅ | Unified agent-run state machine | Stop Review/Document buttons; stop endpoints; stale history cleanup on server startup; continuous history polling; unified Status tab for all three agents |
| 4.8 | ✅ | `documenting` phase in LoopPhase | Loop UI remains active during post-SUCCESS documenter run |
| 4.9 | ✅ | Consistent agent display in CLI (`adapter:model`) | `formatAgent()` helper; same format in config show, project show/list, status |
| 4.10 | ✅ | Test repo setup/cleanup scripts | `scripts/setup-test-repos.sh`, `scripts/cleanup-test-repos.sh` (only touches `/tmp/cfcf-*`) |
| 4.11 | ✅ | Claude Code `--verbose` flag | Shows live turn-by-turn progress in logs, matching Codex's verbose default |
| 4.12 | ⏸ | Diff viewer per iteration | Deferred to iteration 6 (item 6.1) |
| 4.13 | ⏸ | `cfcf log <workspace-name>` CLI | Deferred to iteration 6 (item 6.2). Web History tab covers this for now |
| 4.14 | ⏸ | `cfcf push <workspace-name>` CLI | Deferred to iteration 6 (item 6.3). Manual `git push` works today |
| 4.15 | ⏸ | `cfcf prepare` dry-run command | Deferred to iteration 6 (item 6.4) |
| 4.16 | ✅ | Robust error handling + graceful shutdown | Active process registry (`active-processes.ts`); SIGINT/SIGTERM kills all tracked processes, marks history events and loop states as failed; on startup, stale active loops + running history events are marked failed; unhandledRejection/uncaughtException handlers trigger graceful shutdown; fire-and-forget error handlers now try/catch their own recording logic; watch-mode warning printed at startup; web UI shows improved error banner with hint. 8 new unit tests for registry. |
| 4.17 | ⏸ | Token/cost tracking | Deferred to iteration 6 (item 6.5). Best-effort per iteration per role |
| 4.18 | ✅ | Notification hooks: extensible event-driven notifications | cfcf emits 3 event types (`loop.paused`, `loop.completed`, `agent.failed`) and an extensible dispatcher routes each to configured channels. v1 channels: `terminal-bell` (BEL char to stderr), `macos` (osascript), `linux` (notify-send), `log` (JSON Lines appended to `<project-logs>/notifications.log`). Fire-and-forget with per-channel 5s timeout; one failing channel never blocks another. Global + per-project config in `notifications.events[eventType] = channels[]`. Asked during `cfcf init`. Webhook channel deferred to iteration 5. |
| 4.19 | ⏸ | Cross-project knowledge | Deferred to iteration 5 (item 5.7) — needs memory layer design first. Agent assessments and lessons learned accumulated across projects, with a query interface for context assembly to pull relevant prior knowledge into new projects |
| 4.20 | ⏸ | Tier 3 Strategic Reflection | Deferred to iteration 5 (item 5.6) — build once loop is battle-tested. Reflection agent that periodically reviews the full iteration history (not just the last iteration) and produces pattern analysis, strategy recommendation, and convergence assessment. Output injected into next iteration's context. Configurable frequency via `--reflect-frequency N` |
| 4.21 | ⏸ | Sandbox / guardrails research | Deferred to iteration 6 (item 6.10). Review Anthropic's sandbox concept and evaluate applicability to cfcf's dark factory loop. Consider filesystem scoping, network restrictions, process sandboxing, permission allow-lists |
| 4.22 | ⏸ | Binary self-hosting | Deferred to iteration 5 (item 5.3), paired with template embedding (5.4) and the installer (5.5) |
| 4.24 | ✅ | Per-iteration plan execution prompt (all surfaces) | The one-phase-per-iteration discipline is now injected at **three levels** so it reaches every run, new or existing project, Claude Code or Codex: (1) `context-assembler.generateInstructionContent()` embeds an "Iteration Scope" section in the Tier-1 `CLAUDE.md` / `AGENTS.md` file generated fresh every iteration (this is the authoritative live channel -- reaches existing projects whose `process.md` was copied before the change); (2) the one-line dev-agent CLI prompt in `iteration-loop.ts` now spells out "execute only the next pending chunk from plan.md"; (3) the static `process.md` template (used on first-time project init) has a new "Iteration Scope -- one phase per iteration" section, and the architect's `plan.md` template maps phases to concrete iterations (`## Iteration 1 -- Foundation`). Agent-agnostic: same generated content is written to whichever filename the adapter specifies. Discovered empirically while running the tracker example with a user hint -- captured and promoted into the core prompts so every project gets checkpointed iterations by default. New context-assembler test asserts the discipline is present in both first-iteration and later-iteration instruction files. All repo docs (workflow.md, agent-process-and-context.md, technical-design.md, CLAUDE.md, README.md) updated to reflect the one-phase-per-iteration architecture. |
| 4.25 | ✅ | Live elapsed-time counter on PhaseIndicator | Shared `formatDuration` + `useElapsed` hook (1s tick, no server calls) renders the active agent-run's elapsed time next to the title row (e.g. "Iteration 2 · 2m 14s"). Uses the same format as the History tab's Duration column (now also sourced from the shared util). Hides on completed/failed/stopped, freezes on paused. 9 new util tests in `packages/web/` (first web-package test suite; `test:web` script added at the root). |
| 4.23 | ✅ | Architect review presentation in web UI | The full parsed `ArchitectSignals` (gaps/suggestions/risks/recommended_approach) is now persisted inline on `ReviewHistoryEvent.signals` — the repo file `cfcf-docs/cfcf-architect-signals.json` is overwritten on every review run, so inline persistence is what makes prior reviews viewable. New `ArchitectReview` React component renders the structured JSON with a readiness badge, guidance banner keyed to readiness (e.g. "Edit problem-pack/ files and rerun Review" for NEEDS_REFINEMENT), and collapsible gaps/suggestions/risks/approach sections. Integrated into Status tab (latest review) and History tab (expandable row via clickable readiness pill). Backward-compatible: pre-4.23 review events without `signals` still render their readiness label as plain text. 4 new unit tests in `project-history.test.ts`. |

**In progress branch:** `iteration-4/web-gui`

**Known issues / lessons**:
- Claude Code's `-p` print mode is silent without `--verbose` (fixed in 4.11)
- Codex CLI flag ordering: `-a never` is global, must precede `exec` subcommand
- `bun --watch` restart during active runs orphans agent processes; loop state persistence (3.8) mitigates
- Each `startLoop` resets `loop-state.json`; historical iterations survive only in git branches. The `history.json` (4.6) solves this for reviews/documents but iterations within a run still reset per run start.

---

### Iteration 5: Distribution + Loop Quality + Reflection

**Goal:** Turn cfcf from a developer checkout into a self-contained installable tool, tighten the loop's default behavior, and add the bigger-picture reflection that individual-iteration judging can't give us. This iteration has two loose themes -- **Distribution** (5.3-5.5) and **Loop quality** (5.1, 5.2, 5.6, 5.7) -- either can be picked up independently; 5.3-5.5 form a tight sequence and should ship together.

| # | Status | Title | Notes |
|---|--------|-------|-------|
| 5.1 | ✅ | `autoDocumenter` + `autoReviewSpecs` config flags | **Shipped in v0.7.1 (main @ `4d0b7c3`, feature via PR #7 @ `5593978`).** **Defaults:** `autoReviewSpecs=false` (current behavior -- Review is an optional user-invoked step outside the loop); `autoDocumenter=true` (current behavior -- Documenter runs automatically on SUCCESS). **Three tiers:** on `CfcfGlobalConfig`, overridable on `ProjectConfig`, overridable per-run via CLI flags (`--auto-review` / `--no-auto-review` / `--auto-document` / `--no-auto-document` / `--readiness-gate <level>`). **`autoReviewSpecs=true` behavior:** Review runs as a pre-loop phase (commits to main, not the iteration branch, since review output is a deterministic input to the loop rather than iteration work); Phase indicator gains a leading `Review (agent)` step; the standalone Review button is **hidden** from the UI (Review becomes part of Start Loop, with a nearby hint explaining it's part of the loop and can be changed in settings); loop is gated on the architect's readiness signal. **`readinessGate`** is a third config key with three levels: `"never"` (review informational, loop always proceeds), `"blocked"` (default; stop only on `BLOCKED`, proceed on `NEEDS_REFINEMENT` with a visible warning), `"needs_refinement_or_blocked"` (strictest; stop on anything but `READY`). **`autoDocumenter=false` behavior:** Loop reaches SUCCESS, skips the documenting phase entirely, enters terminal state; user can run `cfcf document` manually. Phase indicator omits the Document step. **Config surfaces:** keys land in this item; the editable global surface lives in 5.9 (web UI settings page); the editable project surface lives in 6.14 (per-project config editor). `cfcf config edit` already covers CLI-side. Small, self-contained; good warm-up for iter 5 wrap-up |
| 5.2 | ✅ | Auto-delete merged iteration branches | `cleanupMergedBranches` config flag (default `false` — preserves iteration branches for audit). Lives on both `CfcfGlobalConfig` (as a default for new projects) and `ProjectConfig` (per-project override). When `true`, the iteration loop calls `deleteBranch` after a successful auto-merge. Non-fatal on delete failure (logs a warning). Git-manager test + project-defaults test added. |
| 5.3 | ✅ | CLI self-hosting: `cfcf server start` works from the installed CLI without a repo checkout | The CLI re-spawns itself with `CFCF_INTERNAL_SERVE=1` when `cfcf server start` is invoked and the server source file is absent on disk. Detection is file-stat-based. CLI entry point in `packages/cli/src/index.ts` dispatches to `startServer()` directly when the env var is set. Dev mode (`bun run`) keeps the existing `bun run packages/server/src/index.ts` spawn path. Originally verified with a `bun --compile`-built `cfcf-binary` (v0.5.0–v0.9.0); the same mechanism carries over to v0.10.0's npm-format install — the bundled JS is invoked under Bun via the `~/.bun/bin/cfcf` shim. |
| 5.4 | ✅ | Embed all runtime assets (templates + web dist) into binary, with local overrides | (a) **Templates** (`packages/core/src/templates.ts`): all 13 templates imported via `with { type: "text" }` and served through `getTemplate(name, { repoPath? })`. Lookup order: `<repoPath>/cfcf-templates/<name>` → `<CFCF_CONFIG_DIR>/templates/<name>` → embedded. Four runners migrated (`architect`, `judge`, `documenter`, `context-assembler`). 12 unit tests. (b) **Web dist** (`scripts/embed-web-dist.ts` → `packages/server/src/web-assets.generated.ts`, gitignored): builds `packages/web` then produces a generated module exporting a `{ path → { contentType, body } }` map backed by base64. Hono route replaces the prior `serveStatic` call with a handler that serves from the embedded map (SPA fallback to `index.html`). Dev fallback reads from `packages/web/dist/` on disk if the generated file is absent. Verified: running `cfcf` from /tmp with no repo checkout produces a working API + web GUI, asset bytes match the Vite build. (Originally verified with the `cfcf-binary` from `bun --compile`; the embedding mechanism is unchanged in v0.10.0's npm-format install — same generated module is loaded by the bundled JS.) |
| 5.5 | ✅ | Installer + npm-format distribution | **Shipped on `iteration-5/installer` (2026-04-26) → v0.10.0.** Detailed design: [`research/installer-design.md`](../research/installer-design.md) (rewritten 2026-04-26 after the `bun --compile` pivot — `docs/decisions-log.md` 2026-04-26). Distribution shape: cfcf is a standard npm-format CLI package, `@cerefox/cfcf-cli`. Build produces `cfcf-X.Y.Z.tgz` (bundled JS via `bun build` without `--compile` + `package.json` declaring runtime deps). Per-platform native deps (custom-built libsqlite3 with `loadExtension` enabled + sqlite-vec, pinned versions) ship as separate `@cerefox/cfcf-native-<platform>` optional npm packages with `os` + `cpu` fields. Users install via `bun install -g <tarball-URL>` or via the slim `scripts/install.sh` wrapper (bootstraps Bun if missing, fetches the matching native tarball, runs `bun install -g`). Bun ≥ 1.3 is a runtime requirement (cfcf uses `bun:sqlite`, `Bun.spawn`, `Bun.file`, `Bun.serve` directly). Release CI rewritten: `workflow_dispatch` only; per-platform `build-native` matrix + single `build-cli` job + `release` step that uploads tarballs + SHA256SUMS + MANIFEST.txt + install.sh. `cfcf doctor` self-check (11 checks: Bun runtime, package metadata, native package + libsqlite3 + sqlite-vec, custom-libsqlite-loadable, 3 runtime deps, 2 agent CLIs, Clio DB) + `cfcf self-update` CLI (`bun install -g <new-tarball>`) ship as part of 5.5. Phase-0 dogfood verified on Intel Mac (darwin-x64). Phase 2 (publish to npmjs.com when cfcf goes OSS) tracked as 5.5b. |
| 5.5b | ✅ | Publish to npmjs.com (post-iter-5 wrap) | **Shipped 2026-05-01 in v0.16.4.** v0.16.2 was the first npm publish (granular bypass-2FA token); v0.16.3 switched to OIDC trusted publishing + sigstore provenance for all 4 packages; v0.16.4 was the install.sh polish — final design is `npm install -g --prefix ~/.bun @cerefox/codefactory` (curl-bash one-liner wrapper), validated on Apple Silicon + Intel Macs. Full four-design journey + lessons in [`docs/decisions-log.md`](decisions-log.md) (2026-05-01 entry). The 8-step plan below records the path. **Branch**: `iteration-5/npm-publish-5.5b` (started 2026-04-29). Goal: cfcf becomes a standard `bun install -g @cerefox/codefactory` install, with the existing GitHub-Releases tarball path preserved as an offline/pinned fallback. **Naming decision (2026-04-29)**: npm package = `@cerefox/codefactory` (reads cleanly in install instructions; future-proofs the @cerefox scope for sibling packages). CLI command stays `cfcf` (typing-friendly; users invoke 100x/day). No aliases — multi-bin entries cause cross-platform headaches without enough payoff. **Versioning**: 1:1 mirror of git tags (`v0.X.Y` → npm `0.X.Y`). First publish = whatever's current on `main` when 5.5b ships. No backfill of older versions. **8-step plan**: (1) ✅ Plan + audit `release.yml` — `docs/research/npm-publish-5.5b-audit.md` (2026-04-29) captures the audit + 5 confirmed decisions (`@cerefox/codefactory-native-*` rename, dry-run as canary, tag format `vX.Y.Z` → npm `X.Y.Z`, 2FA already on the npm account, `npm publish` not `bun publish`). (2) ✅ Update `package.json` files + npm-required fields + **`os`/`cpu` declared on the main `@cerefox/codefactory` package itself** (`"os": ["darwin", "linux"]` + `"cpu": ["arm64", "x64"]`) so `bun install -g @cerefox/codefactory` on Windows-native or FreeBSD fails fast with `EBADPLATFORM` instead of silently succeeding without the matching native tarball and then crashing at first run (rationale captured in `research/npm-publish-5.5b-audit.md` R4 follow-up — hard-fail at install time beats silent-then-runtime-crash; `cfcf doctor`'s libsqlite3 check stays as a second line of defense; postinstall scripts considered + rejected — disable-able, supply-chain attack surface). Native packages rebrand-aligned to `@cerefox/codefactory-native-*` (confirmed in step-1 audit, 2026-04-29). License switched from `UNLICENSED` to **`Apache-2.0`** (matches the rest of the Cerefox ecosystem); `LICENSE` file added at repo root. Legacy `@cerefox/cfcf-*` resolution fallback dropped from `constants.ts`/`clio/db.ts`/`doctor.ts` for security (a hard cut so the legacy name can never silently take effect at runtime). (3) ✅ Refactor `release.yml` — `publish_to_npm` workflow input (default false); `npm-dryrun` job runs unconditionally as a canary on every release run; gated `npm-publish` job runs real `npm publish --access public` (native packages first, CLI second) when `publish_to_npm=true` AND `npm-dryrun` passed; existing GitHub Release leg unchanged. (4) ✅ `scripts/build-cli.sh` version resolution: positional arg → `CFCF_VERSION` env var → root `package.json` (replaces the hardcoded `v0.0.0-dev`). Build banner reports the source so debugging is trivial. (5) ✅ `scripts/install.sh` defaults to npm; tarball mode auto-engages when `CFCF_BASE_URL` is set or with `CFCF_INSTALL_SOURCE=tarball`. Bun bootstrap + bun-global dedup workaround preserved in both paths. (6) ✅ `cfcf self-update` defaults to `bun install -g @cerefox/codefactory@latest`; same `--source` / `--base-url` / `CFCF_INSTALL_SOURCE` surface as `install.sh`. Latest-version resolution via `https://registry.npmjs.org/@cerefox/codefactory/latest` (no npm CLI dependency). The post-install banner now prompts first-time users to run `cfcf doctor && cfcf init` so direct `bun install -g` users get the same first-run nudge as `install.sh` users. 17 unit tests cover the source/version resolver. (7) ⏳ **Pre-flight test pass**: `gh workflow run release.yml -f tag=v0.X.Y-rc1 -f publish_to_npm=false` — exercises the build matrix + GitHub Release artefacts WITHOUT touching npm. Verify everything works end-to-end before any irreversible step. (8) ⏳ **Real publish (gated, irreversible step the user explicitly approves)**: flip the repo to public; create + add `NPM_TOKEN` GitHub repo secret (granular access token, scoped to `@cerefox/codefactory` + `@cerefox/codefactory-native-*`); run `gh workflow run release.yml -f tag=v0.X.Y -f publish_to_npm=true`; smoke-install in a fresh container (`bun install -g @cerefox/codefactory` + `cfcf doctor`); update `installing.md` + `manual.md` + `cli-usage.md` install snippets to use the new path; tag final release. **Acceptance** (preserved from prior plan + sharpened): (a) end-to-end smoke of the rewritten `release.yml` (steps 7 + 8 above); (b) `@cerefox/codefactory` published to npmjs.com; (c) `install.sh` defaults updated; (d) Cerefox repo on GitHub set to public; (e) `bun install -g @cerefox/codefactory` works in a fresh-machine smoke test. **Blockers cleared**: 5.11/5.12/5.13 (Clio Cerefox parity) ✅; 5.8 (user manual + HA) ✅; 5.14 (PA + iteration-history fix) ✅. **Out of 5.5b** (deferred / not part of this work): 2FA auto-rotation tooling, npm pre-release tags (`@beta`, `@next`), npm provenance attestations (sigstore) — all nice follow-ups once the publish pipeline is stable. |
| 5.6 | ✅ | Tier 3 Strategic Reflection + iterative planning | **Detailed design: [`research/reflection-role-and-iterative-planning.md`](../research/reflection-role-and-iterative-planning.md).** Shipped end-to-end across three commits on branch `iteration-5/reflection-pr1` (tags `v5.6-pr1/2/3`). PR1: `cfcf-docs/iteration-logs/iteration-N.md` artifact written by the dev agent each iteration, `iteration-history.md` now rebuilt from those logs (fixes the loop-restart bug), `decision-log.md` charter expanded to cover dev/judge/architect/reflection/user with timestamp + `[role]` + `[iter]` + `[category]` tags, all four role templates updated. PR2: Reflection role runner (`reflection-runner.ts`) with `runReflectionSync` (loop) and `startReflection` (ad-hoc); `cfcf-reflection-instructions.md` + `cfcf-reflection-signals.json` templates; `ReflectionSignals` + `ReflectionHistoryEvent` + `IterationHealth` types; `reflectionAgent` + `reflectSafeguardAfter` on `CfcfGlobalConfig` / `ProjectConfig` with backfill; `cfcf reflect --project <name> [--prompt "…"]` CLI; `POST /api/projects/:id/reflect`. PR3: loop wiring — new `reflecting` phase, `shouldRunReflection()` trigger helper (judge opt-out + safeguard ceiling of 3), separate `cfcf iteration N reflect` commit for the three-commit discipline, `makeDecision` accepts `reflectionSignals` and honours `recommend_stop` (Q6 precedence), judge template + signals gain `reflection_needed` / `reflection_reason`, decision-log size warning at iter 50, web History tab gets a reflection row with color-coded `iteration_health`. 255 tests passing across core + server + cli + web. |
| 5.7 | ✅ | Cross-workspace knowledge -- codename **Clio** | Persistent agent memory layer. **Shipped on `iteration-5/clio`** as three logical PRs in one branch + multiple post-review refinements. PR1: SQLite schema + FTS5 + migrations runner (bun:sqlite, no native deps), markdown chunker (1:1 Cerefox port), LocalClio backend (CRUD + FTS search + content_hash dedup), HTTP `/api/clio/*` + `PUT /api/workspaces/:id/clio-project`, CLI `cfcf clio {search,ingest,get,projects,project,stats}` + `cfcf memory` alias, `cfcf workspace init --project` + interactive Clio Project picker + `cfcf workspace set --project [--migrate-history]`. PR2: embedder catalogue (**nomic default** post-2026-04-22 for 8k context, plus bge-small / MiniLM / bge-base), `@huggingface/transformers@3.8.1` + `onnxruntime-node@1.21.0` (pinned for darwin-x64 support), ONNX wrapper with lazy HF download to `~/.cfcf/models/` + warmup, active-embedder tracking (migration 0002), hybrid RRF + pure-vector search, small-to-big sibling expansion, **Cerefox-style cosine threshold** (`clio.minSearchScore`, default 0.5, FTS-matched chunks bypass), `cfcf clio embedder {list,active,install,set}` (with `set --reindex` for safe model switches), brute-force cosine in TS (sqlite-vec deferred — see 6.15). PR3: iteration-loop auto-ingest at reflect/architect/decision-log/iteration-summary boundaries (respecting `workspace.clio.ingestPolicy`), `cfcf-docs/clio-relevant.md` context preload, `cfcf-docs/clio-guide.md` agent cue card. **Post-review refinements**: `cfcf clio reindex` (idempotent, batched, per-Project), `cfcf clio docs list` (browse), `cfcf init` numbered-list agent picker + inline embedder install with progress bar + post-install verification, `--mode` + `--min-score` CLI flags, `clio.defaultSearchMode` + `clio.minSearchScore` config (visible/editable in the web UI's Server Info page), workspace-scoped `--migrate-history` (default safe; `--all-in-project` opts back into the wide sweep), error classifier in init that distinguishes module-resolution vs network failures. **Research:** [`docs/research/cross-project-knowledge-layer.md`](../research/cross-project-knowledge-layer.md). **Design:** [`docs/design/clio-memory-layer.md`](../design/clio-memory-layer.md). **Implementation decisions:** [`docs/research/clio-implementation-decisions.md`](../research/clio-implementation-decisions.md). **Test plan:** [`docs/research/clio-test-plan.md`](../research/clio-test-plan.md). **Decisions log entries** (top of [`docs/decisions-log.md`](../decisions-log.md)): "Hybrid search threshold (Cerefox port)" + "Clio embedders: model-source, version pinning, and platform support". **Release:** v0.9.0. 383 tests passing (350 core + 22 server-clio + 9 cli + 2 web). **v2 follow-ups tracked separately as items 6.15-6.18 (sqlite-vec, audit log, soft-delete + versioning, web Clio tab).** Remote Cerefox backend stays in the backlog (Future Iterations section). |
| 5.8 | ✅ | User manual (embedded + CLI + doc) + CLI verb normalisation + tab completion + Help Assistant | A canonical user manual covering the full workflow (setup → problem pack → review → run → reflect → document → extend). Four sub-PRs on `iteration-5/cli-verb-normalisation` and follow-up branches. **PR1** ✅ (2026-04-27, v0.12.0): CLI verb normalisation — proposal in [`docs/research/cli-verb-normalisation.md`](../research/cli-verb-normalisation.md), decision in [`docs/decisions-log.md`](../decisions-log.md), three-clause rule applied to the Clio surface. No deprecation aliases. **PR1b** ✅ (2026-04-27, v0.13.0, branch `iteration-5/cli-completion-and-ci-fixes`): tab completion (`cfcf completion {bash,zsh,install,uninstall}`), GitHub Actions hardening, iter-6 plan entry 6.24 (Clio FTS title boosting), auto-install completion across all install paths, sentinel-marked rc-file auto-edit, `cfcf doctor` check for completion wiring, manual-setup detection. **PR2 + PR3** ✅ (2026-04-27, branch `iteration-5/user-manual`, COMBINED): (a) `docs/guides/manual.md` (NEW, hub-and-pointer style) + `docs/guides/troubleshooting.md` (NEW, dedicated diagnostics page, full "tab doesn't fire / cfcf server won't start / Clio search returns nothing / oh-my-zsh interactions / worst-case clean reinstall" coverage), (b) `cli-usage.md` Clio section restructured into per-verb subsections + new "Shell completion" section, (c) `installing.md` refresh, README "Start here" callout, (d) `scripts/embed-help-content.ts` generator embeds `docs/guides/*.md` + `docs/api/server-api.md` into `packages/core/src/help-content.generated.ts` (gitignored), (e) `cfcf help [topic]` CLI command with slug + alias resolution + `--list`, (f) `/api/help/topics` + `/api/help/topics/:slug` HTTP routes, (g) web UI Help tab (top-bar `Help` link, two-pane layout, minimal Markdown renderer that rewrites intra-doc `.md` links to navigate the tab in-place), (h) `cfcf doctor` "User manual + help content" check. **PR4** ✅ (2026-04-27, v0.15.0, branch `iteration-5/help-assistant`): **Help Assistant** (`cfcf help assistant`). Design rewritten 2026-04-27 from the original "Ask the Agent" one-shot concept (now superseded; see [`ask-the-agent.md`](../research/ask-the-agent.md)) to an **interactive cf² role** that runs the user's configured dev agent (claude-code / codex) with a curated system prompt + the embedded help bundle + Clio memory access. Launches in the current shell (no new terminal — the agent's TUI takes over until exit). Read-only by default; mutations gated by the agent's per-command permission prompt. Persists user preferences across sessions via Clio memory Projects (`cfcf-memory-ha` + `cfcf-memory-global`). HA is the **first** role using the role-based-agent architecture; **the Product Architect role (item 5.14) reuses the same launcher seam with Pattern B**. Full design: [`docs/research/help-assistant.md`](../research/help-assistant.md). Shipped: `cfcf help assistant` CLI subcommand, `helpAssistantAgent` config field (default = `devAgent`'s adapter, claude-code → haiku / codex → `/fast` hint), system-prompt assembler embedding the help bundle, Pattern A injection (`--append-system-prompt` for claude-code, `model_instructions_file` tempfile for codex), Clio memory schema (Projects only, no extra metadata), Levenshtein "did you mean?" for typo'd `cfcf help <topic>`, `cfcf doctor` HA-prereq check. |
| 5.11 | ✅ | Clio — update-doc API + versioning + soft-delete (Cerefox parity, **prerequisite for 5.5b**) | **Shipped on `iteration-5/clio-update-api` (2026-04-26).** Promoted from 6.17. (a) `IngestRequest.documentId` + `IngestRequest.updateIfExists` + `IngestRequest.author` accepted by `LocalClio.ingest`, `POST /api/clio/ingest`, `cfcf clio docs ingest` (`--document-id`, `--update-if-exists`, `--author`) — mirrors `cerefox_ingest(document_id=…, update_if_exists=…, author=…)`. (b) `clio_document_versions` writes: snapshot current chunks into a new version row, bump `version_number`, archive prior chunks by setting `version_id` to the new row (FTS triggers handle in/out automatically). The version row's `source` column records the OUTGOING author so `cfcf clio docs versions` answers "who wrote v3?" without JOINing the audit log. (c) `cfcf clio docs versions <doc-id>` + `GET /api/clio/documents/:id/versions` list archived versions newest-first. (d) `cfcf clio docs get <doc-id> [--version-id <uuid>]` + `GET /api/clio/documents/:id/content` reconstruct full content for live or archived versions. (e) UNIQUE constraint on `content_hash` dropped so updates whose new content matches another doc's hash don't deadlock (originally migration `0003_relax_content_hash`; subsequently folded into the consolidated `0001_initial.sql` 2026-04-27). (f) `IngestResult.action` (`"created"`/`"updated"`/`"skipped"`) is the source of truth; legacy `created` preserved one release. (g) `cfcf clio docs delete <id>` (soft-delete) + `cfcf clio docs restore <id>` + `DELETE /api/clio/documents/:id` + `POST /api/clio/documents/:id/restore` — Cerefox parity. Idempotent restores/deletes; `cfcf clio docs list --include-deleted` opts back into showing tombstones. **Out of 5.11**: retention cleanup of old version rows (small follow-up; pairs with 5.13's audit retention). |
| 5.12 | ✅ | Clio — agent-parity API surface (Cerefox `AGENT_QUICK_REFERENCE.md` parity) | **Shipped on `iteration-5/clio-update-api` (2026-04-26).** (a) `author` field threads end-to-end: typed column on `clio_documents` (originally migration `0004_author_column`; folded into the consolidated `0001_initial.sql` 2026-04-27), persisted on creates + updates, surfaced on `getDocument`, `search` (new `SearchHit.docAuthor`), `listDocuments`, `metadata-search`, `audit`. Default `'agent'` for backwards compat with PR1 ingests. (b) `metadata-search`: `cfcf clio metadata search --filter '{"role":"reflection"}' [--updated-since <iso>] [--project <name>] [--include-deleted]` + `POST /api/clio/metadata-search`. Top-level scalar matches; supports the catch-up workflow Cerefox `cerefox_metadata_search` exposes. (c) `metadata-keys`: `cfcf clio metadata keys [--project <name>]` + `GET /api/clio/metadata-keys`. Aggregates keys + value samples (max 5 per key); most-used first. Mirrors `cerefox_list_metadata_keys`. (d) Search-result rendering: every hit now prints `[id: <full-uuid>]` + `author: <name>` so the `search → ingest --document-id` agent workflow works end-to-end without manual ID lookup. |
| 5.13 | ✅ | Clio — audit log wiring (Cerefox parity) | **Shipped on `iteration-5/clio-update-api` (2026-04-26).** Promoted from 6.16. Schema was already in place from 0001's `clio_audit_log` placeholder. (a) `LocalClio.writeAudit` writes one row per mutation: `create`, `update-content`, `edit-metadata`, `delete`, `restore`, `migrate-project`. Idempotent no-op deletes/restores/edits do NOT write extra rows. Reads (search, get, list) intentionally NOT recorded — the volume would be noisy and the trust story is about writes. Best-effort: write failures are warned to stderr but don't fail the mutation. (b) `cfcf clio audit [--event-type <e>] [--actor <name>] [--project <p>] [--document-id <id>] [--since <iso>] [--limit <n>]` + `GET /api/clio/audit-log` with the same filters. Newest first; mirrors `cerefox_get_audit_log`. Vocabulary aligned with Cerefox's `operation` enum. (c) **Metadata-only edit** (2026-04-27 follow-up): `MemoryBackend.editDocument(id, edits)` + `PATCH /api/clio/documents/:id` + `cfcf clio docs edit <id> [--title …] [--author …] [--project …] [--set-meta k=v]… [--unset-meta k]…` lets agents and users mutate title/author/project/metadata without re-ingesting content. **No version snapshot is taken** — versions exist to protect chunks from accidental overwrite, and metadata edits don't touch chunks. Idempotent no-ops write no audit row; otherwise one `edit-metadata` audit entry carries a before/after diff of the changed fields. Set/unset semantics for metadata (vs Cerefox's full-blob replace) avoid the read-modify-write footgun where an agent accidentally drops keys it didn't know about; a future `CerefoxRemote` adapter can reconstruct the full blob from these deltas if upstream demands it. **Out of 5.13**: retention auto-cleanup of old audit rows + version rows (a small follow-up; pairs with the same write-path edits). |
| 5.10 | ✅ | Rename cf² `project` → `workspace` (prerequisite for Clio / item 5.7) | **Shipped in v0.8.0 (feature branch `iteration-5/workspace-rename`).** Clean break -- no CLI or API aliases. User-facing surfaces renamed: `cfcf project *` → `cfcf workspace *`; `--project` flag → `--workspace`; `/api/projects/*` → `/api/workspaces/*` with `projectId` / `projectName` response fields → `workspaceId` / `workspaceName`; web "Projects" nav → "Workspaces"; `#/projects/:id` → `#/workspaces/:id`. Types renamed in both core + web: `ProjectConfig` → `WorkspaceConfig`, `ProjectStatus` → `WorkspaceStatus`, `dispatchForProject` → `dispatchForWorkspace`, `getProject*` / `listProjects` / `createProject` / `findProjectByName` / `updateProject` / `deleteProject` / `validateProjectRepo` → `getWorkspace*` / `listWorkspaces` / …, `getProjectLogDir` / `ensureProjectLogDir` → `getWorkspaceLogDir` / `ensureWorkspaceLogDir`, `getProjectDir` / `getProjectsDir` → `getWorkspaceDir` / `getWorkspacesDir`. Modules renamed: `projects.ts` → `workspaces.ts`, `project-history.ts` → `workspace-history.ts`, `commands/project.ts` → `commands/workspace.ts`, `ProjectDetail.tsx` → `WorkspaceDetail.tsx`, `ProjectCard.tsx` → `WorkspaceCard.tsx`, `ProjectHistory.tsx` → `WorkspaceHistory.tsx`. Templates: `{{PROJECT_NAME}}` → `{{WORKSPACE_NAME}}`. On-disk config directory moved from `<cfcf config dir>/projects/` to `<cfcf config dir>/workspaces/` -- platform-specific path, no auto-migration (user deletes the old directory once verified). `NotificationEvent.project` → `NotificationEvent.workspace`. 255 tests passing. See [`docs/research/workspace-rename-plan.md`](../research/workspace-rename-plan.md) for the full rename surface. |
| 5.9 | ✅ | Editable global settings page + top-bar nav | **Shipped in v0.7.3 (feature branch `iteration-5/settings-editor`).** Top bar gains `Projects` + `Settings` links; `#/server` page promoted to an editor with four sections (agent roles, iteration defaults, behaviour flags, notifications matrix); wire-compatible with `cfcf config edit` via new `PUT /api/config`; server-owned fields (version, permissionsAcknowledged, availableAgents) preserved regardless of client input; explicit banner "this edits global defaults; per-project overrides in 6.14". Original description below for reference. Promote the existing read-only `#/server` page to a full **global-settings editor**. Top bar gains a `Settings` link (next to the logo) that opens it, and optionally a `Projects` link alongside it for discoverability (currently only reachable via the root route). The editor uses the same endpoints the CLI already hits (`GET /api/config`, `PUT /api/config`) so CLI and web stay wire-compatible. Banner at the top of the page makes the scope explicit: **this edits the global defaults; per-project overrides live in each project's Config tab, which is read-only today and becomes editable in 6.14.** Exposes every key the CLI's `cfcf config edit` covers: dev/judge/architect/reflection/documenter agents + models, `maxIterations`, `pauseEvery`, `reflectSafeguardAfter`, `cleanupMergedBranches`, `autoReviewSpecs`, `autoDocumenter`, `readinessGate`, notifications. Depends on 5.1 for the three new keys. |
| 5.14 | ✅ | Product Architect role (PA) — first-class SDLC role, peer to dev / judge / Solution Architect / reflection / documenter | **Shipped via PR #22 (`iteration-5/product-architect-spec`).** v1 (Pattern B) landed in `0f154a9`; v2 refactor to Pattern A + disk+Clio memory in `b2822b3`; multiple follow-on dogfood fixes (workspace-registration symlink fix, project-agnostic Clio reads, per-turn memory writes, Status-tab integration, "PA → Product Architect" UI expansion, etc.). All v1-scope deliverables listed below are done. Items in "Open questions for v3+ dogfood" are explicit future work, not blockers. **Full design baseline**: [`docs/research/product-architect-design.md`](../research/product-architect-design.md). PA is the cf² Product Architect / Owner / Manager — interactive, user-facing, owns everything before the Solution Architect picks up. Sits at the front of the cf² SDLC flow: `cfcf workspace init` → `cfcf spec` (PA) → `cfcf review` (Solution Architect) → `cfcf run` (loop) → `cfcf reflect` → `cfcf document`. **Verb**: top-level `cfcf spec [task...]` (decided 2026-04-28); peer to `cfcf review` / `cfcf reflect` / `cfcf document`, NOT under `cfcf help`. **Interactive by design** (TUI takeover, like the Help Assistant) — unlike `cfcf review` / `cfcf reflect` / `cfcf document` which are fire-and-forget. **Primary scope**: repo setup (offers `git init`), workspace registration (insists on `cfcf workspace init` before memory writes; collects name in conversation, runs the command non-interactively), Problem Pack authoring + iteration + review (`<repo>/problem-pack/{problem,success,constraints,hints,style-guide}.md` + `context/`). **Secondary scope** (allowed with control nudges): `cfcf server start`, `cfcf run` (with "you'll get better control + visibility running it yourself" framing — control primary, cost a dimension not a refrain), reading logs, status checks, answering cf² questions. **Out of scope** (hard refuse): playing dev / judge / Solution Architect / reflection / documenter; writing `plan.md`. **System-prompt injection**: Pattern A (ephemeral, regenerated each session by cfcf at launch with full state injection + memory inventory + the full ~160 KB cfcf docs bundle). v1 originally used Pattern B (durable `<repo>/cfcf-docs/{AGENTS,CLAUDE}.md`); v2 switched to Pattern A because durability is now handled by the disk + Clio memory model. **Memory protocol** (disk + Clio hybrid): `<repo>/.cfcf-pa/` is PA's working memory cache (low-latency, instant disk writes during interactive use) — files: `session-<id>.md` (live scratchpad, one per session), `workspace-summary.md` (local mirror of Clio's `pa-workspace-memory`), `meta.json` (sync metadata). Clio is canonical: `pa-workspace-memory` (per-workspace, in `cfcf-memory-pa` Project) + `pa-global-memory` (cross-workspace, lives ONLY in Clio, in `cfcf-memory-global` Project, shared with HA). Sync at session start (timestamp reconciliation; pull or push as needed) + at session end (PA proactively asks "save before you go?"). PA reads (read-only) other-role Clio Projects (`cfcf-memory-reflection`, `cfcf-memory-architect`, `cfcf-memory-ha`) for cross-role context. **Pre-injection at launch**: cfcf computes + injects git status, workspace registration lookup, server status, iteration history, problem-pack file states, `.cfcf-pa/` state, memory inventory, `session_id`. **v1 scope**: `packages/core/src/product-architect/` module (prompt-assembler + workspace-state reader + memory reader + Pattern A launcher); `productArchitectAgent` config field on `CfcfGlobalConfig` (backfilled to `architectAgent`'s adapter); `cfcf spec [task...]` top-level CLI command; init/config-show/web Server-Info plumbing; `cfcf doctor` "Product Architect prerequisites" check; tests. **Open questions for v3+ dogfood**: cross-workspace memory expansion, PA monitoring loops actively, web UI surface (see 5.15), session-file rotation policy in `.cfcf-pa/`. |
| 5.15 | ✅ | Web UI integration investigation for HA + PA roles | **Researched 2026-05-01.** Full writeup: [`docs/research/pa-ha-web-ui-investigation.md`](../research/pa-ha-web-ui-investigation.md). **Findings**: (a) Web UI today already covers L0/L1 for PA (History row + 3-tab detail panel + Status-tab "active" indicator with the explicit "(interactive — runs in user's terminal)" caveat); HA has zero web coverage. (b) Industry survey: no mainstream AI-coding-agent CLI ships a primary web UI as of 2026 — Cursor/Windsurf/Zed are IDE-first, Claude.ai/ChatGPT are chat-first, Claude Code/Codex/Aider are CLI-first. Teams that explicitly chose NOT to integrate (Docker Desktop, kubectl + Lens, GitHub CLI vs github.com) cite "terminal-in-app is a deep permanent cost, users have a terminal they like." (c) Five patterns surveyed (cast-and-replay, status mirror, shadow chat, web-terminal embed, two-surface agent); cfcf already does status mirror well for PA. (d) Surprise finding: agent CLIs now expose protocols beyond stdio (Claude Code `--input/output-format=stream-json`, Codex `mcp-server` + experimental `app-server`/`exec-server` + WebSocket-to-remote-app-server) — meaning a future shadow-chat or web-terminal path has *less* protocol-engineering work than a year ago, but doesn't change the v1 recommendation. **Recommendation**: Option B (small additive bundle, ~2 days) — **B1**: add HA to History (it's invisible today); **B2**: surface PA's Clio writes in the detail panel (data is already in `meta.json`, just not rendered); **B5**: HA "Help Assistant" button per page that prints the CLI command in a modal (per existing HA design doc). Defer B3 (asciinema-style transcript) + B4 (live elapsed-time indicator) until those gaps are felt. **Explicitly out of v1 scope**: xterm.js / PTY bridge (Option D), shadow-chat / Jupyter-pattern session sharing (Option E), two-surface agent (Option F) — listed in the doc with cost/value/when-right per option. **Trigger for v1 implementation**: maintainer's discretion, can land in iter-6 or be deferred further. The research deliverable is complete; v1 implementation is a separate plan entry to be opened when work begins. |

**Dependency map for this iteration:**
- `5.3 → 5.5` and `5.4 → 5.5` (distribution chain)
- `5.5 → 5.8` (manual's quick-start should reference the installer path)
- `5.1 → 5.9` (5.9 exposes the 5.1 keys in the global-settings editor)
- `5.9 → 6.14` (6.14 mirrors 5.9's editor pattern for the workspace tier)
- `5.10 → 5.7` (Clio uses `workspace` terminology so Cerefox's `Project` stays Cerefox's; the rename must land first)
- `5.8 (PR4 HA) → 5.14` (PA reuses HA's launcher seam — Pattern A/B dispatch lives where HA already cut the seam)
- `5.14 → 5.15` (web UI research happens AFTER PA v1 ships so the investigation can compare both roles in their CLI form before recommending a browser-friendly surface)
- All others (`5.2`, `5.6`) are independent and can be picked in any order

**Candidate scope reductions (if we want a small iteration 5):**
- **Distribution-only iter 5**: ship 5.3 + 5.4 + 5.5 and defer the rest to iter 6. Cleanest "0.5.0 is installable" story.
- **Loop-quality-only iter 5**: ship 5.1 + 5.2 + 5.6 and defer distribution. Keeps the tool in developer-checkout mode but makes each run smarter.
- **Full scope**: ship all seven. Larger iteration; acceptable if appetite is there.

---

### Iteration 6: ADLC research + Clio rationalisation + CLI/web parity + UI polish

**Goal** (locked 2026-05-02 after the iter-6 cleanup pass):

1. **Research stream** — generalise cfcf into a configurable Agent Development Life Cycle harness for non-coding iterative work (**6.11**, headline; research-only, doc at `docs/research/adlc-extension-design.md`), with scheduled execution as the operational complement (**6.13**, research-only, doc at `docs/research/scheduled-execution-design.md`).
2. **Clio rationalisation across agent roles** (**6.9** reframed) — standardise how each role queries and writes Clio (semantic + FTS over `grep`-the-repo), document the canonical patterns.
3. **CLI ↔ web parity audit** (**6.12**) — gap analysis driving any missing endpoints; absorbs the originally-separate 6.2 (`cfcf log`) question.
4. **Web UI polish** — Clio browser tab (**6.18**), new-version notification (**6.20**), per-role model dropdowns sourced from each agent's supported-models list (**6.26**).
5. **Cerefox-parity Clio improvement** — FTS title boosting (**6.24**).

**Iter-6 active set after the cleanup**: 6.9, 6.11, 6.12, 6.13, 6.18, 6.20, 6.24, 6.26 (plus 6.19 partial: pre-warm-during-installer). Items 6.1, 6.2, 6.10, 6.15 dropped to ⏸ (with rationales in their Notes columns); 6.8 marked ❌ but blocked on 6.11. See the status legend at the end of the section for what each icon means.

| # | Status | Title | Notes |
|---|--------|-------|-------|
| 6.1 | ⏸ | Diff viewer per iteration | **Dropped from iter-6 2026-05-02** — superseded by F.13 (rich diff viewer with annotation, in Backlog). The annotation layer is the differentiating value over plain syntax-highlighted diff; building 6.1 first would require redoing the whole surface as part of F.13 later. CLI users today have `git diff main..cfcf/iteration-N`; web users have the History tab + iteration-logs/handoffs/reviews archives. If demand for a richer in-UI diff materialises, F.13 is the canonical record. |
| 6.2 | ⏸ | `cfcf log <workspace-name>` CLI | **Dropped from iter-6 2026-05-02** — subsumed by 6.12 (CLI ↔ web parity audit). Today's web History tab + `cfcf-docs/iteration-history.md` cover read-on-demand needs; manufacturing a dedicated `cfcf log` command before knowing what gap it closes is feature-shaped. Let the parity audit drive scope: if it surfaces a real CLI deficiency, scope a command then. |
| 6.3 | ⏸ | `cfcf push <project-name>` CLI | **Dropped 2026-04-20.** Intended to wrap `git push` with project awareness + notification hooks, but (a) post-SUCCESS push is already handled automatically by the iteration loop (`gitManager.push(project.repoPath)` at end of a successful loop), (b) manual pushes are trivially `git push` from inside the repo -- no cfcf state needed, (c) the project-awareness value disappeared when `ProjectConfig.repoUrl` was removed in v0.7.5 (it was the only cfcf-specific piece the command would have consulted). Retaining the row as ⏸ so the decision is preserved; the surrounding CLI ↔ web parity question is still covered by 6.12. |
| 6.8 | ❌ | Custom directions per role + process template versioning | **Resolved 2026-05-02 — blocked on 6.11.** Don't schedule independently in iter-6: the two-tier design is sound (see below), but the ADLC research (6.11) will sharpen what role-customisation actually needs to support — committing to a customisation surface before 6.11 risks a shape that doesn't fit ADLC's role abstraction. Pick up 6.8 as a follow-up to 6.11 once the role model is settled. **Two-tier design (preserved for reference)** that avoids fork-vs-upgrade pain: *(Tier 1, default for everyone)* cfcf ships current templates and upgrades them in place -- user gets new defaults on `cfcf` upgrade, no migration step. *(Tier 2, opt-in additional-directions block)* each role template gains a documented insertion point; if the user provides `cfcf-templates/custom-directions/<role>.md` (project-local) or `<CFCF_CONFIG_DIR>/templates/custom-directions/<role>.md` (user-global), cfcf splices it verbatim into the generated instruction file at that point, **in addition to** the default template. The block is user-owned -- cfcf never touches it on upgrade, so migration pain goes away because the defaults don't fork. Examples: "Documenter: use our internal report format, include X section"; "Dev: every commit message must reference a JIRA ticket ID"; "Architect: always include an accessibility review section". *(Tier 3, power-user fallback)* the existing full-template override path (`cfcf-templates/<name>.md`) remains available, documented as "you own the upgrade path for any template you fork". **Versioning piece:** track which cfcf version's defaults the project was created with, for debugging. |
| 6.9 | ❌ | Rationalise Clio usage across agent roles | **Reframed 2026-05-02** from "Optional Cerefox memory backend" — Clio shipped in 5.7 (local SQLite-backed memory layer with FTS + hybrid search). The new scope: **standardise how each agent role queries and writes Clio**, document the canonical patterns, ensure roles use semantic/hybrid search rather than grepping repo files. The main Clio benefits are (a) cross-workspace persistent memory and (b) efficient retrieval (semantic + FTS) — much more efficient than `grep` over `*.md` in the repo, especially as cfcf-docs and decision-log grow. **Scope**: audit each role's instructions template (dev / judge / architect / reflection / documenter / PA / HA) for current Clio guidance; rewrite to a consistent "search Clio for X before doing Y" pattern; document agent-side Clio patterns in `docs/research/`; possibly extract a shared "Clio cheat sheet" that gets injected into every role's prompt. **Out of scope**: a remote `CerefoxRemote` backend adapter (the original 6.9 item) — that stays optional/future, the `MemoryBackend` interface already exists for it. **Clio Project namespace convention (surfaced 2026-05-02 during 6.12 dogfooding)**: a few projects today have de-facto special meaning — `default` is the catch-all global bucket; `cfcf-memory-pa` is owned by the Product Architect; `cfcf-memory-global` is the cross-workspace shared bucket. Today every web picker (workspace creation, "Change Clio Project", Memory page sidebar) lists ALL projects unfiltered, including these system-managed ones. 6.9 should formalise the convention: pick a naming prefix (e.g. `cfcf-memory-*`) or an explicit registry of "system projects", document who owns each, and decide which projects are user-pickable vs. system-only at the picker site. Once the convention lands, web pickers add a one-line filter; until then, the unfiltered list is fine but users may pick a system project by mistake. |
| 6.10 | ⏸ | Sandbox / guardrails research + POC | **Dropped from iter-6 2026-05-02** — moved to Backlog F.5 as durable record. Threat model that would justify sandbox engineering (untrusted Problem Pack from a third party? cfcf-as-a-service?) doesn't exist today: users run cfcf on their own machines under their own account; iteration isolation is already provided by git branches; Codex `--dangerously-skip-permissions` + Claude Code's matched mode give the unattended-loop permission profile cfcf needs. If/when a multi-tenant scenario emerges, macOS `sandbox-exec` / Linux `nsjail` / Anthropic's sandbox concept are the right primitives — research with concrete use cases at that point. F.5 in the Backlog carries the trigger note. |
| 6.11 | ❌ | Generalisation research → Agent Development Life Cycle (ADLC) extension | **Iter-6 headline; research-only scope locked 2026-05-02.** Extend cfcf to non-coding iterative work, framed as researching a **parallel/configurable workflow alongside the current SDLC**, hardwired into the harness rather than bolted on. End game: refactor cfcf into a more dynamic, configurable harness where the SDLC is one of N built-in workflows (ADLC, content production, research projects, data analysis, strategy planning). **Research questions**: Is the Problem Pack shape general enough across workflow types? Does the dev / judge / architect / reflection / documenter role split map to non-coding workers, or do roles need to be workflow-specific too? Does the signal schema need workflow-specific shapes (no tests to pass for content production)? What's the "success criteria" surrogate when the artifact is a report rather than passing tests? **Output (iter-6)**: research doc at `docs/research/adlc-extension-design.md` enumerating options, decision points, and a concrete proposal. **Implementation deferred to iter-7+** — committing to a first-implementation stretch in iter-6 would commit to a deadline before the design is settled; if the research lands fast we'll re-evaluate at that point. Likely overlaps with the Cerefox Agent v0.1 vision. Pairs with 6.13 (scheduled execution) for the periodic-content use case. **6.8 (custom directions per role) is blocked on this** — pick up 6.8 once 6.11 settles the role model. |
| 6.12 | ❌ | CLI ↔ web-GUI parity audit | After 5.6 ships `cfcf reflect`, cfcf's CLI surface covers `init`, `server start/stop/status`, `workspace init/list/show/delete`, `config show/edit`, `run`, `resume`, `stop`, `review`, `document`, `reflect`. Audit every command for a matching web-GUI action; document gaps; plan any missing endpoints. Pairs naturally with 6.2 (`cfcf log`) and 6.3 (`cfcf push`) which surface the same parity issue. Surfaced by §13 of the reflection design doc. |
| 6.13 | ❌ | Cron-like recurring execution research | **Iter-6 scope locked 2026-05-02 — research only. Builds on the `JobScheduler` primitive shipped in 6.20** (so iter-7+ implementation is "extend the primitive" rather than "build from scratch"; the research doc should validate the existing primitive's interface against 6.13's needs and propose extensions: cron-expression parsing, user-defined jobs, schedule-management UX, concurrency / mutex policy). Output: `docs/research/scheduled-execution-design.md` enumerating options + recommended design. Implementation is iter-7+, gated on 6.11 ADLC landing because schedule's natural mode is non-coding workflows (periodic reports, continuous research, scheduled audits). Research and design a scheduling layer so cfcf can fire a project on a recurring cadence -- e.g. "every weekday at 08:00 run a fresh loop against the updated Problem Pack and produce a report." Useful for coding/devops as much as for content/research. **Representative use cases:** *(a) Content + research (pairs with 6.11):* periodic reports, newsletter digests, continuous research with an evolving Problem Pack. *(b) Coding / devops:* weekly CVE + security-update sweeps on the dependencies of a deployed repo (flag to user, or auto-patch + run tests + open a PR + optionally trigger the CI/CD pipeline that deploys); scheduled dependency updates (non-breaking minor/patch bumps, deprecated-API audits → update → test → PR → notify); recurring health checks (dead-link scans, broken-build early-warning, doc-drift audits, license/SBOM refreshes). **Output gate (user-chosen at schedule-definition time, guided by CLI + web UI), two modes:** *(1) PR mode* -- cfcf opens a PR against the default branch and waits for the user to review/merge; *(2) Direct-commit mode* -- cfcf commits directly to the default branch (main) under a user-specified content folder (e.g. `reports/weekly-cve/<date>.md`), optionally tagged, optionally pushed to the remote when one is configured. The user picks per-schedule. Default CLI/UI guidance should favour PR mode for side-effecting / customer-visible work and direct-commit for append-only content (reports, digests, audit logs) where no code-path changes happen. **Open design questions:** Per-project cron expressions vs. per-trigger schedules? Does a scheduled run mean a fresh iteration loop, a `cfcf reflect` pass, an architect re-review, or a new Problem Pack ingest -- or is it user-selectable per schedule? How do completed loops interact with the next scheduled run (reset to main? carry state?)? How do we surface schedule history in the web UI? Does cfcf run its own scheduler (macOS `launchd` / systemd timers / `node-cron`) or integrate with the user's existing one? What guardrails prevent runaway costs if a schedule fires while the previous run is still active? Output: a research doc under `docs/research/` enumerating options + recommended design, not an implementation. |
| 6.14 | ✅ | Editable per-project config in the web UI | **Shipped in v0.7.4 (feature branch `iteration-5/project-settings-editor`, pulled forward from iter 6 since 5.9 set the editor pattern).** Project detail's `Config` tab is now a full editor: identity + runtime fields (id, name, repo path, status, iterations completed, process template) stay read-only at the top; below it, four editable sections (Remote URL / Agent roles / Iteration defaults incl. onStalled + mergeStrategy / Behaviour flags / Notifications override with explicit "inherit global" toggle). Wire-compatible via `PUT /api/projects/:id` which gained validation + identity-preservation. Per-project notifications can be cleared by sending `notifications: null`. Original description below for reference. Counterpart to 5.9 for the project tier. The project detail page's existing `Config` tab is read-only today; make it editable. Writes via the existing `PUT /api/projects/:id` endpoint. All per-project fields become editable (agents + models per role, maxIterations, pauseEvery, onStalled, mergeStrategy, cleanupMergedBranches, reflectionAgent, reflectSafeguardAfter, autoReviewSpecs, autoDocumenter, readinessGate, notifications override). Banner at the top of the page makes the scope explicit: "These override the global defaults. Global settings live in the top-bar Settings link." Depends on 5.9 so users have a consistent editor pattern + banner tone across the two surfaces. |
| 6.15 | ⏸ | Clio v2 — sqlite-vec HNSW integration | **Dropped from iter-6 2026-05-02** — moved to Backlog F.18 with corpus-size trigger ("Clio corpus >10k chunks OR brute-force cosine >50ms p95"). Today's Clio corpora are sub-1k chunks; brute-force cosine is sub-10ms; ANN is theoretical until corpora grow. Data shape is already right (embedding BLOB column) so the work stays purely additive whenever we revisit. F.18 carries the trigger note; full original scope preserved there. |
| 6.16 | ↗ | Clio v2 — audit log wiring | **Promoted to 5.13** on 2026-04-26. See that row for current scope. |
| 6.17 | ↗ | Clio v2 — update-doc API + versioning + soft-delete | **Promoted to 5.11** on 2026-04-26 after the agent-parity gap analysis. See that row for current scope. |
| 6.18 | ❌ | Clio v2 — Web UI tab | New web UI surface for browsing Clio. Projects list + doc count per project, per-project document grid with filter chips (role / artifact_type / tier / workspace), hit viewer that reconstructs a doc from its chunks (existing `GET /api/clio/documents/:id` + a new expand-chunks helper), search box that hits `GET /api/clio/search`. Top-bar gets a "Memory" link next to "Workspaces" + "Settings". Depends on 6.16 for the audit log's "recent activity" panel. |
| 6.19 | 🔄 | Clio v2 — `cfcf init` Clio onboarding polish | **Shipped 2026-04-22:** pick-equals-install (the embedder chosen during `cfcf init` downloads + activates inline rather than just saving a preference), `cfcf clio embedder install` arg made optional (defaults to `clio.preferredEmbedder`), stderr progress bar during HuggingFace download via `progress_callback` (one-line-per-file, ≥5% ticks), pre-download bandwidth hint (size + ETA anchored at 50/10 Mbps), post-install auto-verification (reads `clio_active_embedder` back, fails hard if the row doesn't match, prints `✓ Clio ready: <name> (dim=N, chunk=N chars)`). **Still pending:** pre-warm during installer (5.5) so fresh installs ship with the default model already cached -- removes first-`cfcf init` download cost for the users who take the default. |
| 6.20 | ✅ | New-version notification (web UI + CLI banner) + minimal JobScheduler primitive | **Shipped in v0.18.0 on `iteration-6/version-notification`.** **Design doc**: [`docs/design/scheduler-and-update-notification.md`](../design/scheduler-and-update-notification.md). **Surfaces shipped**: (a) `JobScheduler` primitive in `packages/core/src/scheduler/` with `Job` interface (`{ id, intervalMs, fn, runOnStart?, lastRun?, lastError? }`), single periodic tick (60 s), state persisted to `~/.cfcf/scheduler-state.json` after every run; (b) built-in `update-check` job with `runOnStart: true` so every server boot fires a fresh npm registry check (24 h interval is the long-running-server safeguard cadence) — when `latest > running`, writes `~/.cfcf/update-available.json` (`{ currentVersion, latestVersion, checkedAt }` — no clickable URL for security: `~/.cfcf` is user-writable so an attacker-controlled link would be a phishing surface in the web UI); (c) `clearStaleUpdateFlag(VERSION)` runs at `startServer()` boot as offline-safe defense-in-depth so disconnected upgrades still clear the banner; (d) web UI top-bar `<UpdateBanner />` rendered above `<Header />`, dismissible per-`latestVersion` via `sessionStorage`; piggybacks on the new shared `ServerStatusProvider` context (one poll cycle covers health + activity + update-status, no per-component timer); (e) CLI lifecycle banner on `init` / `server` / `status` / `doctor` / `self-update --check` only — bold yellow on TTY (with `NO_COLOR` + non-TTY fallback), suppress via `notifyUpdates: false` config or `CFCF_NO_UPDATE_NOTICE=1`; (f) `GET /api/update-status` returns 200+JSON when newer, 204 otherwise (defensive `latestVersion <= VERSION` guard on read); (g) `cfcf doctor` "Update availability" check; (h) `notifyUpdates?: boolean` added to `CfcfGlobalConfig` (default true, backfilled by `validateConfig`). **Tests added**: 43 new (scheduler tick + persistence + `runOnStart`, semver compare + flag-file lifecycle + stale-flag GC, `/api/update-status` HTTP route, CLI banner lifecycle gating + suppression + color rendering). **Out of scope (deferred to 6.13)**: cron-expression parsing, user-defined jobs, schedule-management UX, mutex/concurrency. **No auto-update under any circumstance** — user always runs `cfcf self-update` explicitly. **Original design notes (preserved for reference)**: Design refined 2026-05-02 (after the iter-6 cleanup). Two refinements vs. the strawman "wire a one-off timer for update-check": **(refinement #1) generalise the periodic-check into a `JobScheduler` primitive** so 6.13 (cron-like recurring execution) extends it later rather than building a parallel timer mechanism. **(refinement #2) limit the CLI banner to lifecycle commands** (`init` / `server` / `status` / `doctor` / `self-update --check`) instead of every `cfcf <verb>` invocation — every-invocation prints add 5–20ms FS-read overhead to scripted operations and become noise users filter out. **Implementation surface**: *(a)* `packages/core/src/scheduler/` — `Job` interface (`{ id, intervalMs, fn, lastRun? }`), `JobScheduler` class with single periodic tick (~60s), state persisted to `~/.cfcf/scheduler-state.json` for restart resilience. *(b)* Built-in `update-check` job, 24h interval, hits `https://registry.npmjs.org/@cerefox/codefactory/latest` (GH Releases as fallback); on a newer-than-running version, writes `~/.cfcf/update-available.json` with `{ latestVersion, checkedAt, releaseNotesUrl? }`. *(c)* Web UI: top-bar banner "v0.X.Y available — run `cfcf self-update --yes` to upgrade", dismissible per-session; always-on (zero per-invocation cost). *(d)* CLI: lifecycle-command allowlist (`LIFECYCLE_COMMANDS = new Set(["init", "server", "status", "doctor"])` + `self-update --check`); each lifecycle command opts in by name, the rest stay silent. Suppress via `notifyUpdates: false` config key or `CFCF_NO_UPDATE_NOTICE=1` env var. *(e)* `GET /api/update-status` endpoint surfacing the flag to the web UI. **Out of scope (defer to 6.13)**: cron-expression parsing (`0 8 * * MON`), user-defined jobs (CLI `cfcf schedule add`), schedule-management web UI, mutex/concurrency around running jobs (6.20's one job does a tiny HTTP fetch — revisit if needed). **No auto-update under any circumstance** — user always runs `cfcf self-update` explicitly. **Background**: CLI `cfcf self-update` shipped in 5.5; this item is the proactive notification layer that nudges users without being intrusive, plus the scheduler primitive 6.13 will reuse. Pairs with 6.18 (web UI Clio tab) since the same top-bar surface holds both banners. **Branch**: `iteration-6/version-notification`. **Estimated effort**: 1–2 sessions. |
| 6.24 | ❌ | Clio FTS title boosting (Cerefox parity) | **Surfaced 2026-04-27.** Cerefox FTS weights the document title + chunk heading at column-A and the body at column-B (`setweight(to_tsvector(doc_title), 'A') \|\| setweight(to_tsvector(content), 'B')`), so query terms appearing in titles outrank body-only matches. cfcf-Clio currently indexes only the chunk content + heading_path in `clio_chunks_fts`; doc title isn't stored as a separately-weighted FTS column at all, and FTS5's `bm25()` weight argument is unused. **Scope**: (a) Extend `clio_chunks_fts` with a `doc_title` column, populate from `clio_documents.title` on insert/update (FTS5 trigger or app-side). (b) `LocalClio.searchFts` calls `bm25(clio_chunks_fts, weight_doc_title, weight_chunk_title, weight_content)` with title columns weighted 3-5× higher than body — exact ratio tuned against the corpus. (c) `clio_documents` title updates (rename via `cfcf clio docs edit --title`) trigger FTS re-population for all the doc's live chunks (analogous to Cerefox's `cerefox_update_chunk_fts`). (d) On-demand reindex pass for existing chunks ingested before this change: `cfcf clio reindex --refts` (parallels `--rechunk` from 6.23). **Why iter-6 not now**: the user remembers Cerefox's title-boost design absorbed significant tuning effort; cfcf benefits from doing this once with eyes open rather than rushing in alongside the verb rename. May land alongside 6.15 (sqlite-vec) since both reach into the FTS / search-engine path. **Reference**: `../cerefox/src/cerefox/db/migrations/0011_title_boosting.sql` for the original implementation; `../cerefox/src/cerefox/db/rpcs.sql` for the runtime weighting. Cerefox uses Postgres `tsvector` weights (A/B/C/D); SQLite FTS5's per-column `bm25()` weights are the equivalent knob. |
| 6.21 | ✅ | `cfcf init --force` reads existing config as defaults | **Shipped on `iteration-5/installer` (2026-04-26).** When `--force` is passed against an already-configured machine, init now reads the existing config and uses it as the base rather than starting from `createDefaultConfig`. Each role's adapter is healed against the current `available` list (a previously-installed agent that's since been removed falls back to the bootstrap default). Per-role model prompts default to the existing model. Embedder picker defaults to the existing `clio.preferredEmbedder` rather than `DEFAULT_EMBEDDER_NAME`. `cfcf config edit` is unchanged because it already delegates to `cfcf init --force` and inherits the fix transparently. |
| 6.25 | ✅ | Structured pause actions (resume-action enum + contextual button matrix) | **Shipped 2026-05-02 on `feat/structured-pause-actions`.** **Design doc**: [`docs/research/structured-pause-actions-design.md`](../research/structured-pause-actions-design.md). **Decision-log lessons**: [`docs/decisions-log.md`](../decisions-log.md) (2026-05-02 entry). **Surfaced 2026-05-01** during dogfooding the tracker workspace: user typed "Judge and Reflection agree, proceed to Documentation" at a pause prompt; harness silently misrouted the text into the next dev agent's prompt as "guidance" and fired iter-6. The pre-fix pause UX had one input channel (free-text textarea) and one behavior (resume → next dev iteration), which silently misrouted 3 of 5 common user intents. **Shipped scope**: `ResumeAction` enum (`continue` / `finish_loop` / `stop_loop_now` / `refine_plan` / `consult_reflection` — final naming locked 2026-05-01); free text routes per action (dev / documenter / audit-history / architect / reflection); `pauseReasonAllowedActions(pauseReason, signals?)` helper as single source of truth for UI button visibility + CLI validation; reflection signals gain `harness_action_recommendation` field; new `loop-stopped` history event type; documenter runner accepts optional `userFeedback`; reflection runner gains `consultMode` + `userFeedback` opts; `runLoop` switches on `state.resumeAction` at top (early-out for stop/finish, synchronous architect for refine_plan, consult-mode reflection for consult_reflection); web UI `FeedbackForm` rewritten as contextual button matrix with no bare "Resume"; CLI gains `--action` with `choices()` validation; API accepts `action` field on resume body (defaults to `continue` for back-compat). 10 new unit tests covering the matrix (43 total in `iteration-loop.test.ts`, was 33). **Compat**: no breaking changes — pre-6.25 CLI/API callers default to `action: "continue"` and behave identically. |
| 6.26 | ❌ | Per-role model selection: dropdowns sourced from each agent's supported-models list | **Surfaced 2026-05-02.** Today the per-role model field is a free-text input in both the web Settings + workspace Config tabs (and a free-text prompt in `cfcf init` / `cfcf config edit`). Easy to typo, no validation, no discoverability of what models the installed agent CLI actually supports. **Scope**: replace the free-text model field with a dropdown in (a) web Settings → Agent roles, (b) workspace Config → Agent roles, (c) CLI `cfcf init` / `cfcf config edit` model prompts (numbered selector matching the agent picker pattern, with a "(other — type a model name)" fallback). **Source of options**: ask each detected agent CLI for its supported-models list at adapter-detection time (the same code path that today populates `availableAgents`). Each adapter gains a `listModels(): Promise<string[]>` method on `AgentAdapter` (or null if the upstream CLI doesn't expose one); the result is cached on `CfcfGlobalConfig` as `availableModels: Record<adapterName, string[]>`. **Refresh strategy**: re-query the agent CLI before opening any model picker (web: on Settings/Config tab mount; CLI: at the top of `cfcf config edit` and `cfcf init --force`) so a recent agent-CLI upgrade that added a new model is picked up without manual cache busting. The cached list is the fallback for offline / slow paths. **Open questions to resolve when picking this up**: (1) does the Claude Code CLI expose a list-models subcommand? what about Codex? if not, do we hardcode a per-adapter "known models" registry in the adapter itself + document the upstream-changes workflow? (2) should "(use adapter default)" stay an explicit option (it's the current free-text empty-string semantic) — yes, by analogy with the agent-role dropdown's "auto" handling; (3) does the web allow free-text fallback too, in case a user wants to try an unreleased model — recommended yes, with a "Custom model name…" sentinel option that swaps to a text input (mirrors the pattern shipped in 6.12's ClioProjectDialog); (4) per-workspace overrides: same dropdown surfaces in WorkspaceDetail's Config tab, sourced from the same cached list. **Tests**: round-trip the dropdown → save → fetch flow per adapter; cache invalidation on agent-CLI version bump; "Custom model" fallback for unknown values that came from a hand-edited config file. **Background**: `AgentAdapter.detect()` already exists; this just adds `listModels()` next to it. The web ConfigDisplay agent-role table is already a `<select>` for adapter names — adding a parallel `<select>` for the model field is a small extension. **Effort estimate**: 1-2 sessions including the adapter API addition + research on what each agent CLI actually exposes. |

**Status icons (this section):**
- ✅ Shipped
- ❌ Not started — accepted scope, ready to pick up
- 🟡 Needs discussion / review — design or scope is open and should be talked through before scheduling
- 🔄 Partially shipped — see Notes for what landed and what remains
- ⏸ Paused — kept for posterity; superseded or out-of-scope (decision recorded in Notes)
- ↗ Promoted to a different iteration (see Notes)

(Items that were ❌ → 📦 moved to Backlog or 💧 dropped during the 2026-05-02 cleanup are listed in the Backlog section below, not in this iter-6 table. Original 6.x numbers preserved in their Backlog entries for traceability.)

---

## Backlog

Single bucket for items that aren't on the active iteration plan. Includes:
- **Concrete deferrals** — items with prior scope from earlier iterations, deferred to wait for a trigger condition or because the workaround is sufficient.
- **Speculative themes** — vaguer ideas worth tracking but not designed in detail; gathered here from the historical "Future Iterations (v0.7+)" section that the 2026-05-02 cleanup folded in.

Each entry uses an `F.x` number (sequential, append-only). When an item came from a prior plan row, the original number is noted in the Notes column for traceability. Append new items at the bottom; never renumber.

| # | Title | Notes |
|---|-------|-------|
| **F.1** | Third agent adapter (Aider / OpenCode / Goose / …) | *(Was 6.6.)* Validates the plugin interface with a meaningfully different agent. Trigger: user demand for a non-claude-code / non-codex adapter, or a clear plugin-interface validation gap. Today claude-code + codex cover the workload. |
| **F.2** | Multi-workspace concurrent execution in server | *(Was 6.7.)* Concurrent execution via worker threads or separate child processes. Trigger: user runs into the single-active-loop constraint (two cfcf loops on different workspaces simultaneously). Today's single-active-loop is fine for solo-dev usage. |
| **F.3** | Clio version retention (Cerefox parity, on-demand cleanup) | *(Was 6.22.)* `cfcf clio cleanup-versions [--older-than 168h] [--keep-last 1]` + the `archived` flag wiring. Trigger: corpora grow to where storage matters, or version-noise in the audit log becomes annoying. Eventually want full Cerefox parity. |
| **F.4** | Clio chunker re-chunk on embedder switch (`cfcf clio reindex --rechunk`) | *(Was 6.23.)* The `--rechunk` flag isn't implemented; the warning recommends it but says "manual re-ingest" until then. Trigger: a user actually switches embedders and the `--reindex` workaround feels insufficient. **Drop candidate** if no demand surfaces. |
| **F.5** | Sandbox / guardrails research + POC | *(Was 6.10, currently 🟡 in iter-6 — moved here pending decision.)* Trigger: cfcf becomes a service, or any multi-tenant scenario emerges. Currently no use case (users run on own machines, own permissions). If iter-6 discussion confirms drop, this entry sticks here as the durable deferral record. |
| **F.6** | Token / cost tracking | *(Was 6.5, dropped 2026-05-02.)* cfcf spawns agents as separate processes; agent CLIs don't reliably emit machine-readable usage data after each turn, so cost tracking would need adapter-specific stdout-scraping that's brittle across agent versions. Reconsider if/when an agent CLI exposes a structured usage API. |
| **F.7** | `cfcf prepare` dry-run command | *(Was 6.4, dropped 2026-05-02.)* Debug-context-assembly is covered by reading `cfcf-docs/` files directly. Reconsider if a real debugging workflow demands it. |
| **F.8** | Coordinator Agent Pattern | A main agent launches + directs sub-agents within each iteration; role-based assignment (planner / coder / tester / documenter); agent-to-agent communication via cfcf message bus. Replaces fire-and-forget with richer inner execution. **Significant overlap with 6.11 ADLC research** — likely subsumed or informed by that work; revisit after 6.11 lands. |
| **F.9** | Iteration branching (parallel approach exploration) | Run multiple iteration approaches in parallel and pick the best. Speculative; would need a cost/benefit assessment given the agent-runtime cost multiplier. |
| **F.10** | Agent switching mid-run based on reflection output | Reflection currently rewrites the plan but doesn't switch the dev agent. Speculative refinement of the role-config-per-iteration model. |
| **F.11** | Incremental success (partial test pass acceptance) | Currently judge is binary (SUCCESS / PROGRESS / STALLED / ANOMALY). Speculative: gradient acceptance for projects where partial completion is meaningful. |
| **F.12** | Automatic problem decomposition | An agent breaks a large Problem Pack into smaller workspaces/loops. Speculative; pairs with 6.11 (ADLC) and F.8 (Coordinator). |
| **F.13** | Rich diff viewer with annotation | Annotated diff per iteration with reviewer notes. Possibly subsumes 6.1 (diff viewer, currently 🟡 in iter-6) — annotation layer would be the differentiating value over plain syntax-highlighted diff. |
| **F.14** | Dependency + call-graph visualisation | Show the codebase's structural shape in the web UI. Speculative. |
| **F.15** | Notifications integrations: Slack / WhatsApp / email / webhooks | Beyond the current terminal-bell + macOS/Linux desktop notifications. The notification dispatcher is already pluggable (`packages/core/src/notifications/`); these would be additional channels. |
| **F.16** | Homebrew formula | Alternate distribution channel for macOS users who prefer `brew install` over the curl-bash one-liner. Trigger: enough macOS demand to justify the homebrew-tap maintenance overhead. |
| **F.17** | Remote execution (SSH to a build server) | cfcf runs on a remote machine while the user drives from their laptop. Counter to the "local processes" design choice but possible as a deployment mode for teams. Speculative. |
| **F.18** | Clio sqlite-vec HNSW integration | *(Was 6.15, moved here 2026-05-02.)* Replace the pure-TS brute-force cosine (`LocalClio.searchSemantic` / `searchHybrid`) with `sqlite-vec`'s native ANN. Requires solving the Bun-on-macOS `loadExtension`-disabled-on-Apple-SQLite issue — cleanest path ties into the installer (the installer ships a pinned SQLite + sqlite-vec per-platform, `Database.setCustomSQLite(...)` points at it via `CFCF_SQLITE_LIB`). Benefit: HNSW index is sub-10ms at 1M+ chunks vs. brute-force's linear scan. Data is already in the right shape (embedding BLOB column) — purely additive. **Trigger condition**: Clio corpus exceeds ~10k chunks OR brute-force cosine p95 exceeds ~50ms. Today's corpora are sub-1k chunks (sub-10ms), so ANN is theoretical until growth materialises. |

---

## Decision history

For all design / architectural / process decisions made on cfcf — chronological, with context, options considered, decision, and lessons learned — see [`docs/decisions-log.md`](decisions-log.md). That file is the single source of truth; the previous one-line decision table at the bottom of this plan was deleted on 2026-05-02 to remove duplication and drift risk.

